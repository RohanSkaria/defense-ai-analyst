{
  "name": "defense-ai-analyst",
  "version": "0.1.0",
  "private": true,
  "description": "Defense Knowledge Graph + AI Analyst Agent",
  "scripts": {
    "build": "turbo run build",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^1.12.0",
    "typescript": "^5.3.0",
    "vitest": "^1.3.0"
  }
}
