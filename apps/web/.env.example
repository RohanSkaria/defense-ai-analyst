# =============================================================================
# DEFENSE AI ANALYST - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# -----------------------------------------------------------------------------
# REQUIRED: Anthropic API Configuration
# -----------------------------------------------------------------------------
# Get your API key from: https://console.anthropic.com/
# Used for: Document ingestion, entity extraction, and Q&A analysis
# Models used: claude-haiku-4-5 (fast, cost-effective)
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# REQUIRED: PostgreSQL Database URL
# -----------------------------------------------------------------------------
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?sslmode=require
#
# Examples:
# - Neon: postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
# - Local: postgresql://postgres:password@localhost:5432/defense_ai
# - Railway: postgresql://postgres:xxx@containers-us-west-xxx.railway.app:5432/railway
#
# Note: The application will automatically configure SSL settings based on NODE_ENV
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# -----------------------------------------------------------------------------
# OPTIONAL: Node Environment
# -----------------------------------------------------------------------------
# Set to "production" for production deployments
# - production: Enables strict SSL validation, hides detailed error messages
# - development: Allows self-signed certificates, shows detailed errors
# Default: development
NODE_ENV=development

# -----------------------------------------------------------------------------
# OPTIONAL: Application Configuration
# -----------------------------------------------------------------------------
# These are configured in next.config.ts but can be overridden if needed

# Maximum request body size (already set to 10MB in next.config.ts)
# BODY_SIZE_LIMIT=10485760

# API timeout in seconds (already set to 30s in route handlers)
# MAX_DURATION=30
